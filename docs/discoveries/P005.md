# Discovery Report — P005: session-banner port

**Date:** 2026-06-09
**Branch:** feat/P001-scaffold-cli

## Anchor verification (Task 0)

| # | Assumption | Result | Evidence |
|---|-----------|--------|---------|
| 12 | `src/io.rs`: `ALLOW=0` | CORRECT | `src/io.rs:44` — `pub const ALLOW: i32 = 0;` |
| 13 | `src/hooks/mod.rs`: stub `session_banner()` returns ALLOW | CORRECT | `src/hooks/mod.rs:539-541` — stub confirmed |
| 14 | No `chrono`/`time` dep in Cargo.toml | CORRECT | `Cargo.toml:20` — `regex = "1"` only; no chrono/time |

All 3 anchors CORRECT.

## Date strategy — manual ISO→epoch (CONFIRMED FEASIBLE)

The `staleness_days(iso, now_epoch)` function uses the Howard Hinnant days-from-civil algorithm (public domain, ~10 lines). Format is fixed UTC `Z`-suffix — no timezone handling, no leap-second concern at day granularity.

**Spot-check verified:**
- `2026-06-09T00:00:00Z` → epoch 1780963200 via Hinnant formula
- `date -j -f "%Y-%m-%dT%H:%M:%SZ" "2026-06-09T00:00:00Z" +%s` → 1780963200 (exact match)
- `staleness_days("2026-06-09T00:00:00Z", 1781568000)` → `Some(7)` (confirmed unit test)

Manual epoch is fully viable. No dep-add required. ĐIỂM KHÓ #1 resolved.

## Git shelling (`branch --merged main`)

Uses `std::process::Command::new("git").args(["branch", "--merged", "main"])` with `.current_dir(&root)`. NOT `sh -c`. Failure → empty vec → nudge section skipped silently. Matches ĐIỂM KHÓ #2 spec exactly.

## Banner text bug F-001 — port verbatim (DO NOT self-fix)

Oracle L178 `Marker:` line reads:
```
Marker: touch .sos-state/architect-active trước spawn architect; rm -f trước spawn worker.
```

This **missing** `touch .sos-state/worker-active` is bug F-001 (documented in `docs/SOS_KIT_FEEDBACK.md`). Port doctrine mandates verbatim copy — the Rust port reproduces the bug exactly.

**Text now lives in 2 places:**
1. `scripts/session-start-banner.sh` (canonical oracle, L178)
2. `src/hooks/mod.rs` (Rust port, `session_banner()` step 9)

**Consequence for future fix:** Fix F-001 MUST update BOTH locations atomically, plus `orchestrator.md` and `docs/ORCHESTRATION.md`. Fixing only `scripts/session-start-banner.sh` without the Rust port = drift. This is documented at the call-site in `src/hooks/mod.rs`.

## Render hook fail-open divergence

`session_banner()` ALWAYS returns `ALLOW` (exit 0). Every failure path (no BACKLOG, no `^##`, fs error, git error, parse error) → fail-open, silent. This is the **OPPOSITE** of `block_unsafe_merge()` (fail-CLOSED). Future hook authors must explicitly choose which pattern applies. The divergence is documented in `docs/ARCHITECTURE.md` and the code comment.

## stdin NOT read

`read_payload()` is NOT called. The oracle (`scripts/session-start-banner.sh`) never reads stdin — it renders from file/git state. All 3 block hooks (P002/P003/P004) read stdin; session-banner does not. The `io.rs` harness is unchanged.

## ━ character count discrepancy (Tầng 2 self-decided)

Phiếu stated "58-char ━ lines". Actual oracle count: **60 characters** (verified via `python3 len()`). Oracle is the truth (CLAUDE.md Port doctrine #2 "Bash reference = oracle"). Used 60. This is Tầng 2 (parity vs. spec wording — oracle wins, no architectural impact). No escalation needed.

## Scope expansions

None. Implemented exactly what the phiếu specified.

## Edge cases / limitations found

1. **`git branch --merged main` in non-git dir:** Returns empty (fail-open). Integration tests run in a temp dir which is not a git repo — `git` fails → nudge section silently skipped. This is correct behavior (oracle L108 pattern).
2. **Phiếu cleanup nudge branch-matching:** Oracle L127 uses `grep -qE "/${phieu_id}-"` on merged branches string. The Rust port uses `String::contains(&format!("/{phieu_id}-"))`. Semantically equivalent since branches are already trimmed.
3. **50001-byte file → `doc_size_warns` yields "48k":** 50000/1024=48 (integer division). Matches oracle L84.

## Docs updated to match reality

- `docs/ARCHITECTURE.md`: session-banner stub→real; new section with render pipeline, fail-open/stdout/no-stdin note, date manual-epoch note, F-001 note, pure helper list; Data Flow updated.
- `CHANGELOG.md`: v0.5.0 entry for P005.

## Tier escalations

None. Phiếu stayed Tầng 1 throughout. No 2→1 upgrade triggered. No dep-add escalation (manual epoch feasible).
