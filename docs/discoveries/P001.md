# Discovery Report — P001: Scaffold CLI

**Date:** 2026-06-09
**Branch:** feat/P001-scaffold-cli
**Status:** shipped

---

## Anchor Verification (8/8 CORRECT)

| # | Assumption | Result | Notes |
|---|---|---|---|
| 1 | `src/main.rs` = Hello-world stub | CORRECT | `fn main() { println!("Hello, world!"); }` — replaced entirely |
| 2 | Cargo.toml has clap 4 derive, serde/serde_json, anyhow, thiserror, regex, tokio, rmcp 1.7 | CORRECT | All present; clap has `features = ["derive"]` at L14 |
| 3 | dev-deps assert_cmd 2 + predicates 3 | CORRECT | Both present in `[dev-dependencies]` |
| 4 | `docs/ticket/` directory exists | CORRECT | Pre-verified by Quản đốc |
| 5 | `tests/` not yet created | CORRECT | Created new `tests/cli.rs` |
| 6 | `scripts/architect-guard.sh` fail-open at L44 | CORRECT | `[ -z "$PATH_ARG" ] && exit 0` — mirrored in `read_payload()` |
| 7 | `scripts/block-env-edit.sh` fail-open at L23,35 | CORRECT | `if [ -z "$INPUT" ]; then exit 0` — mirrored in `read_payload()` |
| 8 | Binary name = `claude-hooks` | CORRECT | `[package] name = "claude-hooks"` at Cargo.toml:2 |

---

## Module Names Chosen (for P002+ reference)

- `src/io.rs` — shared harness (chose `io.rs` over `payload.rs`; keeps it consistent with Rust stdlib `io` pattern)
- `src/hooks/mod.rs` — all 4 stub functions in single file (not split 1 file/hook; minimal for scaffold)
- `src/serve.rs` — MCP stub at top-level (not `src/hooks/serve.rs`)

---

## Clap Kebab-Case: Auto-Derived (No Explicit `#[command(name=...)]` Needed)

clap 4.6 auto-converts `ArchitectGuard` → `architect-guard`, `BlockEnvEdit` → `block-env-edit`, etc. Verified via `cargo run -- --help` output and all 8 CLI tests passing.

---

## Dead-Code Suppression Strategy

`ToolInput` fields, `HookPayload.tool_input`, `BLOCK` constant, and `block()` function are intentionally forward-declared for P002+. Suppressed with `#[allow(dead_code)]` at item level. This is correct: `#[allow(unused)]` on `_payload` in hooks handles the local variable side; struct/const/fn-level `#[allow(dead_code)]` handles the module-level side. Both `cargo build` and `cargo clippy -- -D warnings` pass clean.

---

## Scope Expansions

None. Shipped exactly to phiếu spec.

---

## Docs Updated

- `docs/ARCHITECTURE.md`: CLI surface section (5 subcmds, stdin-JSON harness shape, exit-code convention 0/2, fail-open semantics, module structure, MCP stub, data flow, Bash reference table).
- `CHANGELOG.md`: P001 entry added.

Tầng 1 docs updated: `docs/ARCHITECTURE.md` (subcmd add → security-surface per CLAUDE.md DOCS GATE).

---

## Tier Escalations

None.

---

## Tests

8/8 integration tests pass (`cargo test --all`). `cargo clippy -- -D warnings` clean. `cargo build` clean.
